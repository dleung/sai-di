== Sai-Di Website

A quick blogging-style website with dynamicly generated content.  Unfortunately, this project never finished because the client kept changing the frontend design never settled on a final one.
